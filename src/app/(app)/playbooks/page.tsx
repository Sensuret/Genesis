import { ComingSoon } from "@/components/coming-soon";

export default function PlaybooksPage() {
  return (
    <ComingSoon
      title="Playbooks"
      description="Define your trading models with rules (sessions, time windows, RR target, asset focus). Connected to Streaks, Reports and Recaps so you can see if you followed your plan."
      phase="Phase 4.5"
    />
  );
}
