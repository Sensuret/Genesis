import { ComingSoon } from "@/components/coming-soon";

export default function GsInsightsPage() {
  return (
    <ComingSoon
      title="GS Insights"
      description="Triangular GS Score, score performance over time, win % / profit factor / avg win-loss trends, and contextual insights — replaces the old Backtesting page."
      phase="Phase 4"
    />
  );
}
