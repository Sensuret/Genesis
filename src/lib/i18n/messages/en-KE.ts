/**
 * English (Kenya). Identical to en-US for now (mostly the same
 * spelling as en-GB). KES currency formatting is handled by Intl,
 * not the message tree, so this stays small.
 */
import type { MessageTree } from "../types";

const messages: MessageTree = {};

export default messages;
