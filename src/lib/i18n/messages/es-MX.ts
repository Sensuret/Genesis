/**
 * Español (México). Mostly identical to es-ES; only the regional
 * differences (e.g. "computadora" vs "ordenador", forms of address)
 * are overridden here.
 */
import type { MessageTree } from "../types";
import esES from "./es-ES";

const messages: MessageTree = { ...esES };

export default messages;
