/**
 * English (United Kingdom). Identical to en-US for now — we'll add
 * Britishisms (Personalise / Customise / colour vs color) here as we
 * encounter them in the UI. The t() helper falls back to en-US for
 * any missing key, so a partial bundle is fine.
 */
import type { MessageTree } from "../types";

const messages: MessageTree = {};

export default messages;
